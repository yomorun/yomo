package server

var ()

/*
// TestServer New a YoMo Server.
func TestServerNew(t *testing.T,conf *WorkflowConfig, opts ...Option) {

	options := newOptions(opts...)
	return &serverImpl{
		conf:        conf,
		meshConfURL: options.meshConfURL,
	}
}


// TestServerServe serves a YoMo server.
func TestServerServe(t *tesing.T ) {
	handler := NewServerHandler(r.conf, r.meshConfURL)
	server := quic.NewServer(handler)

	return server.ListenAndServe(context.Background(), endpoint)
}

// TestServerServeWithHandler serves a YoMo Server with handler.
func  TestServerServeWithHandler(endpoint string, handler quic.ServerHandler) error {
	server := quic.NewServer(handler)

	return server.ListenAndServe(context.Background(), endpoint)
}
*/
