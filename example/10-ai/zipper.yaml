name: ai-zipper
host: 0.0.0.0
port: 9000

auth:
  type: token
  token: Happy New Year

bridge:
  ai:
    server:
      addr: localhost:8000
      endpoints:
        chat_completions: /chat/completions
      provider: azopenai

    providers:
      azopenai:
        api_key: <YOUR_API_KEY>
        api_endpoint: https://<YOUR_ORG_ID>.openai.azure.com/openai/deployments/<YOUR_MODEL_ID>/chat/completions?api-version=2023-12-01-preview

      openai:
        api_key:
        api_endpoint:

      huggingface:
        model:

