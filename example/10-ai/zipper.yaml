name: ai-zipper
host: 0.0.0.0
port: 9000

# auth:
#   type: token
#   token: <CREDENTIAL>

bridge:
  ai:
    server:
      addr: localhost:8000
      endpoints:
        chat_completions: /chat/completions
      credential: token:<CREDENTIAL>
      provider: azopenai

    providers:
      azopenai:
        api_key:
        api_endpoint:

      openai:
        api_key:
        api_endpoint:

      huggingface:
        model:

