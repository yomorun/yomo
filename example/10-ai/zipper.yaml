name: ai-zipper
host: 0.0.0.0
port: 9000

auth:
  type: token
  token: Happy New Year

bridge:
  ai:
    server:
      addr: localhost:8000
      endpoints:
        chat_completions: /chat/completions
      provider: azopenai

    providers:
      azopenai:
        api_key: 8aebebe4e6654104b99f2a75d7f24e88
        api_endpoint: https://c3y.openai.azure.com/openai/deployments/gpt35/chat/completions?api-version=2023-12-01-preview

      openai:
        api_key:
        api_endpoint:

      huggingface:
        model:

