name: ai-zipper
host: 0.0.0.0
port: 9000

auth:
  type: token
  token: Happy New Year

bridge:
  ai:
    server:
      addr: localhost:8000
      provider: azopenai

    providers:
      azopenai:
        api_key: 
        api_endpoint: 

      openai:
        api_key:
        model:

      huggingface:
        model:

